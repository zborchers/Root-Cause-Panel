import React from "react";
import ReactDOM from "react-dom/client";
import PanelInterpreter from "./PanelInterpreter.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PanelInterpreter />
  </React.StrictMode>
);
